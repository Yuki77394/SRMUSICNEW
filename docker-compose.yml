version: "3.9"

services:
  swaggymusic:
    build: .
    container_name: swaggymusic_bot
    restart: always
    command: python3 -m SWAGGYMUSIC
    env_file:
      - .env
    volumes:
      - .:/app
    networks:
      - botnet

networks:
  botnet:
    driver: bridge
